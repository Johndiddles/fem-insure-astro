---
export interface Props {
  name: string;
  link: string;
  styles?: string;
}

const { name, link, styles } = Astro.props;
---

<a
  href={link}
  class={`uppercase font-karla text-lg md:text-sm text-primary md:text-primary md:hover:text-accent duration-300 font-bold ${styles}`}
  >{name}</a
>
